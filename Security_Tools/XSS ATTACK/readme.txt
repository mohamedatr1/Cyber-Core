XSS: Injecting malicious scripts into websites to execute them in the victim's browser and steal their data.

Defense: Sanitizing user input by converting special characters into plain text so the browser displays them instead of running them
